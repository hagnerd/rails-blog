<h1>All Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li><%= link_to tag.name, tag_path(tag) %></li>
<%= link_to "Delete this Tag", tag_path(tag), method: :delete, data: { confirm: "Are you sure you want to delete the tag?" } %>
  <% end %>
</ul>
